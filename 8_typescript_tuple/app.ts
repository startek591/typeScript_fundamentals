let skill: [string, number];
skill = ['Programming', 5];

console.log(skill); // Output: [ 'Programming', 5 ]

let color: [number, number, number] = [255, 0, 0];
console.log(color);

let bgColor, headerColor: [number, number, number, number?];
bgColor = [255, 255, 255];
headerColor = [0, 255, 255];
console.log(bgColor);
console.log(headerColor);
